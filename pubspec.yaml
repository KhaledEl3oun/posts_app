name: posts_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ">=3.5.2 <4.0.0"

dependencies:
  connectivity_plus: ^6.0.5
  flutter:
    sdk: flutter
  get: ^4.6.6
  http: ^1.2.2
  lottie: ^3.1.2
  path: ^1.9.0
  sqflite: ^2.3.3+1

dev_dependencies:
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets: 
    - assets/
